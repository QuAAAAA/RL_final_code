\begin{figure}
    \centering
    \begin{adjustbox}{width=0.94\linewidth,max height=0.23\textheight, center}
        \begin{tikzpicture}[
            node distance=1.08cm,
            posterstep/.style={rectangle, rounded corners=8pt, draw=black, very thick, fill=white, align=center, text width=12.0cm, minimum height=1.75cm},
            control/.style={rectangle, draw=black, very thick, fill=white, align=center, text width=8.6cm, minimum height=1.55cm},
            connector/.style={ultra thick,->,>=stealth}
        ]
            \node[posterstep] (base) {\textbf{Expressive IndexTTS2 prior}\\Chinese/English source languages with controllable emotion};
            \node[posterstep, below=of base] (adapt) {\textbf{Stage 1: Taiwanese adaptation}\\LoRA/adapters learn pronunciation while limiting forgetting};
            \node[posterstep, below=of adapt] (frontend) {\textbf{ASQP + T\^ai-l\^o front-end}\\VA signals, tone-number romanization, and lexical stress markers};
            \node[posterstep, below=of frontend] (policy) {\textbf{Stage 2: GRPO realignment}\\Text-to-semantic policy samples candidate semantic token sequences};
            \node[posterstep, below=of policy] (tts) {\textbf{Emotion-controlled Taiwanese TTS}\\Waveform preserves content while recovering emotion and emphasis};

            \node[control, right=2.2cm of policy, yshift=1.05cm] (emo) {Emotion category\\and VAD intensity rewards};
            \node[control, right=2.2cm of policy, yshift=-1.05cm] (prosody) {Local emphasis\\and ASR content rewards};

            \draw[connector] (base) -- (adapt);
            \draw[connector] (adapt) -- (frontend);
            \draw[connector] (frontend) -- (policy);
            \draw[connector] (policy) -- (tts);
            \draw[connector] (emo.west) -- (policy.east);
            \draw[connector] (prosody.west) -- (policy.east);
        \end{tikzpicture}
    \end{adjustbox}
    \caption{Final-report pipeline: low-resource adaptation followed by ASQP-conditioned RL realignment.}
    \label{fig:method-flow}
\end{figure}
