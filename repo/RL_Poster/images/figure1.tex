\begin{figure}
    \centering
    \begin{adjustbox}{width=0.94\linewidth,max height=0.26\textheight, center}
        \begin{tikzpicture}[
            node distance=1.25cm,
            posterstep/.style={rectangle, rounded corners=8pt, draw=black, very thick, fill=white, align=center, text width=12.2cm, minimum height=2.0cm},
            reward/.style={rectangle, draw=black, very thick, fill=white, align=center, text width=8.8cm, minimum height=1.7cm},
            connector/.style={ultra thick,->,>=stealth}
        ]
            \node[posterstep] (base) {\textbf{High-resource expressive IndexTTS2}\\Chinese expressive prior, speaker timbre, controllable emotion};
            \node[posterstep, below=of base] (adapt) {\textbf{Parameter-efficient Taiwanese adaptation}\\LoRA/adapters learn target-language pronunciation while limiting parameter drift};
            \node[posterstep, below=of adapt] (policy) {\textbf{GRPO emotional realignment}\\Text-to-semantic policy samples candidate semantic token sequences};
            \node[posterstep, below=of policy] (tts) {\textbf{Emotion-preserving Taiwanese TTS}\\Decoded waveform keeps content accuracy, timbre, intensity, and emphasis};

            \node[reward, right=2.4cm of policy, yshift=1.1cm] (emo) {Emotion category\\and intensity rewards};
            \node[reward, right=2.4cm of policy, yshift=-1.1cm] (prosody) {Local emphasis\\and ASR rewards};

            \draw[connector] (base) -- (adapt);
            \draw[connector] (adapt) -- (policy);
            \draw[connector] (policy) -- (tts);
            \draw[connector] (emo.west) -- (policy.east);
            \draw[connector] (prosody.west) -- (policy.east);
        \end{tikzpicture}
    \end{adjustbox}
    \caption{Two-stage adaptation and RL realignment pipeline.}
    \label{fig:method-flow}
\end{figure}
